// Sync - Đồng bộ
// Async - Bất đồng bộ
// Async in JS: setTimeout, setInterval, fetch, XMLHttpRequest, file reading, request animation frame

setTimeout(() => {
    console.log("First")
}, 2000)

console.log("Second")

// Promise